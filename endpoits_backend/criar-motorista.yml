info:
  name: criar-motorista
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/drivers
  body:
    type: json
    data: |2-
       {
              "id": "d14",
              "nome": "lucas",
              "status": "AVAILABLE"
            }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​