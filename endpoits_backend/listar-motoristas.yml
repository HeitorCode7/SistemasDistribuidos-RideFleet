info:
  name: listar-motoristas
  type: http

http:
  method: GET
  url: http://localhost:3000/api/drivers
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
