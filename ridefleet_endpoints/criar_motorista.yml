info:
  name: criar_motorista
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/drivers
  body:
    type: json
    data: |-
      {
        "id": "d11",
        "nome": "Gab",
        "status": "AVAILABLE"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
