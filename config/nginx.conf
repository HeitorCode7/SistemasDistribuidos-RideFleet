events {}

http {
  upstream ridefleet_backend {
    server api1:3000;
    server api2:3000;
  }

  server {
    listen 80;

    location / {
      proxy_pass http://ridefleet_backend;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
    }
  }
}